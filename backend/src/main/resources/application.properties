# MySQL Connection Configuration Settings (Fixed port to 3306)
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=password123

# Hibernate Generation Engine Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 🎯 FIX: Updated to the correct Hibernate 7 strategy naming convention
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect