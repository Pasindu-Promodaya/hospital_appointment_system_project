# MySQL Database Connection Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# Uses environment variable if present, falls back to password123
spring.datasource.password=${MYSQL_PASSWORD:password123}

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# Spring Framework SMTP Gateway Configuration (Notification Engine)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Fallback points securely to your production project email profile
spring.mail.username=${HOSPITAL_EMAIL_USER:communityhospitalprojectfct@gmail.com}
spring.mail.password=${HOSPITAL_EMAIL_PASSWORD:svdminzvsrbilufr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Twilio WhatsApp Sandbox Configuration (Notification Engine)

twilio.account.sid=AC6874e339e870f9a96ef7cc18b991f387
twilio.auth.token=331e8680657a4fc3c094252dc762b058
twilio.whatsapp.number=+14155238886